环境模型
（一）将可以转为lambda表达式写法的都转化为lambda写法，比如let，函数形式参数之类的。
（二）define：用define定义一个符号，也就是在 *当前环境框架内* 建立一个约束，并赋予这个符号
指定的值。
这个指定的值是什么？（根据163页）
(define (square x) (* x x))
转化为如下形式：
(define square 
  (lambda (x) (* x x)))
其中求值(lambda (x) (* x x)),并将符号square约束于这一求值得到的结果。
可见这个 *指定的值* 是对（define square之后的部分求值得到的结果。
与lambda相关的求值有两种情况：1对一个lambda表达式求值，得到一个过程对象
2将一个过程对象应用于实际参数，采用第四步的处理方式。具体方式具体对待。
注意用define约束的这个square符号，是在全局环境内约束的。
(三）创建过程对象
对于一个 *给定环境* （注意是哪个环境，lambda表达式可能会在另一个lambda表达式内，可能会是函数的形式参数）求值一个lambda表达式。
会创建一个过程对象。
代码：lambda表达式的正文
指向环境的指针：这个指针指向哪个环境？就是这个 *给定环境*
（四）将一个过程对象应用于实际参数。
构建出一个新框架，这个新框架的外围环境是？创建过程对象中 指向环境的指针 指向的环境
在这一新框架中，将形式参数约束到实际参数，然后在新框架中求值过程的体。
